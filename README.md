# FILLY
 A unified UI toolkit that renders the same widget (menu, input, etc.)  as a terminal TUI or a native GTK4 window using a single JSON protocol.  One daemon, one core, two faces. Designed for Linux installers (ArtixForge),  CLI tools, and any project needing interactive prompts that work  transparently over SSH, in a terminal, or on a desktop.
